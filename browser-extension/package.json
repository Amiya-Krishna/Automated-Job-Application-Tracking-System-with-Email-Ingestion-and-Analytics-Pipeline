{
  "name": "tracktrail-extension",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "description": "Dev-only package file: lets `npm test` run the extension's unit tests. Chrome ignores it; load this folder unpacked as before.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  },
  "devDependencies": {
    "jsdom": "^30.1.0"
  }
}
