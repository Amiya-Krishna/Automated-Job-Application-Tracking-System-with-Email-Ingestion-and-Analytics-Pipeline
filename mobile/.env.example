# Base URL of the existing backend API (server/). No trailing slash.
#
# Android emulator dev:  http://10.0.2.2:5000
# Physical Android device dev: http://<your-computer's-LAN-IP>:5000
#   ("localhost" on a physical phone refers to the phone itself, not your
#   dev machine — do not use it here.)
# Production: the deployed Render URL, e.g. https://job-application-tracker-portal-o1ls.onrender.com
EXPO_PUBLIC_API_URL=
